import { Session } from '../session';
export declare type Level = 'ERROR' | 'WARN' | 'INFO' | 'DEBUG';
export declare type MidiLevel = 'Input' | 'Output' | 'Both' | 'None';
/**
 * Simple logger implementation including integration with the Bitwig
 * API's preferences system for setting log level, log filtering via
 * regular expressions, and Midi I/O filtering.
 */
export declare class Logger {
    private _levels;
    private _level;
    private _midiLevel;
    private _levelSetting;
    private _filter;
    private _filterSetting;
    private _initQueue;
    private _flushed;
    constructor(session: Session);
    private readonly _ready;
    level: Level;
    filter: string;
    log(...messages: any[]): void;
    error(...messages: any[]): void;
    warn(...messages: any[]): void;
    info(...messages: any[]): void;
    debug(...messages: any[]): void;
    private _log(level, ...messages);
    private _flushQueue();
}
