import { MidiMessage, SimpleMidiMessage } from './MidiMessage';
import { SysexMessage } from './SysexMessage';
export declare class MessagePattern {
    static getPatternStringFromMidiMessage({port, status, data1, data2}: Partial<SimpleMidiMessage>): string;
    static getMidiMessageFromPatternString(pattern: string): Partial<SimpleMidiMessage>;
    port?: number;
    status?: number;
    data1?: number;
    data2?: number;
    string: string;
    regex: RegExp;
    constructor(input: string | Partial<SimpleMidiMessage>);
    toString(): string;
    conflictsWith(pattern: MessagePattern): boolean;
    test(message: MidiMessage | SysexMessage): boolean;
}
