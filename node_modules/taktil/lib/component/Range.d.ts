import { ControlState } from '../control';
import { Component, ComponentState, ComponentParams } from './Component';
export declare type RangeParams = ComponentParams;
export interface RangeState extends ComponentState {
    value: number;
}
export declare abstract class Range<Params extends RangeParams = RangeParams, State extends RangeState = RangeState> extends Component<Params, State> {
    INPUT_DELAY: number;
    state: State;
    memory: {
        [key: string]: any;
    };
    render(): void;
    getControlOutput(): Partial<ControlState>;
    onControlInput(input: ControlState): void;
}
