import { Control, ControlState } from './Control';
import { MidiMessage } from '../midi';
export declare type ChannelPressureState = ControlState;
export declare class ChannelPressure<State extends ChannelPressureState = ChannelPressureState> extends Control<State> {
    enableMidiOut: boolean;
    status: number;
    constructor({port, channel, ...rest}: {
        port?: number;
        channel: number;
        enableMidiOut?: boolean;
        enableCache?: boolean;
        cacheOnMidiIn?: boolean;
    });
    getControlInput({data1}: MidiMessage): State;
    getMidiOutput({value}: State): MidiMessage[];
}
