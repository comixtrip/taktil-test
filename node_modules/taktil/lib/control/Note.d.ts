import { Control, ControlState } from './Control';
import { MidiMessage } from '../midi';
export declare type NoteState = ControlState;
export declare class Note<State extends NoteState = NoteState> extends Control<State> {
    channel: number;
    data1: number;
    useNoteOff: boolean;
    constructor({port, channel, key, ...rest}: {
        port?: number;
        channel: number;
        key: number;
        enableMidiOut?: boolean;
        enableCache?: boolean;
        cacheOnMidiIn?: boolean;
    });
    getControlInput(message: MidiMessage): State;
    getMidiOutput({value}: State): MidiMessage[];
}
