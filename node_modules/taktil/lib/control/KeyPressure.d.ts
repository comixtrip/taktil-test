import { Control, ControlState } from './Control';
export declare type KeyPressureState = ControlState;
export declare class KeyPressure<State extends KeyPressureState = KeyPressureState> extends Control<State> {
    status: number;
    data1: number;
    constructor({port, channel, key, enableMidiOut, ...rest}: {
        port?: number;
        channel: number;
        key: number;
        enableMidiOut?: boolean;
        enableCache?: boolean;
        cacheOnMidiIn?: boolean;
    });
}
