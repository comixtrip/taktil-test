import { Control, ControlState } from './Control';
export declare type ControlChangeState = ControlState;
export declare class ControlChange<State extends ControlChangeState = ControlChangeState> extends Control<State> {
    status: number;
    data1: number;
    constructor({port, channel, control, ...rest}: {
        port?: number;
        channel: number;
        control: number;
        enableMidiOut?: boolean;
        enableCache?: boolean;
        cacheOnMidiIn?: boolean;
    });
}
